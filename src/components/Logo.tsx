import React from 'react';
import electron from '../assets/sidebar/Electron.svg';

export default function Logo(){
    return (
        <img className="logo__image" src={electron} alt="Electron logo" />
        )}